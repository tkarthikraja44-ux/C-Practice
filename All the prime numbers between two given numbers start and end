#include<iostream>
using namespace std;
int main()
{
    int start = 0;
    cin >> start;
    int end = 0;
    cin >> end;
    for(int i=start;i<=end;i++)
    {
        int count =0;
        for(int k=1;k<=i;k++)
        {
            if(i%k==0)
            {
                count++;
            }
        }
        if(count==2)
        {
            cout << i<<" ";
        }
    }
    
}
