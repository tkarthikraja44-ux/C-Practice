#include<iostream>
using namespace std;
int main()
{
    int num = 0;
    cin >> num;
    int sum=1;
    if(num%2==0)
    { 
        cout <<"Enter the Valid  number";
    }
    else
    {
     for(int i=1;i<=num;i+=2)
      {
        sum=sum*i;
      }
      cout << sum;
    }

}
