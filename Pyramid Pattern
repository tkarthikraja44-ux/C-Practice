#include<iostream>
using namespace std;
int main()
{
    int num = 0;
    cin >> num;
    int l=1;
    for(int i=1;i<=num;i++)
    {
        for(int j=1;j<=i;j++)
        {
            cout <<l<<" ";
            l++;
        }
        cout << endl;
    }
}
