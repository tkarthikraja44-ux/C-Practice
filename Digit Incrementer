#include<iostream>
using namespace std;
int main()
{
	int num =0;
	cin >> num;
	int sum = 0;
	while(num>0)
	{
		int digit = num%10;
		if(digit<=8)
		{
			sum = ((sum*10)+digit)+1;
		}
        num/=10;
	}
	int n=0;
	while(sum>0)
	{
		n = (n*10)+sum%10;
		sum/=10;
		
	}
	
	cout << n;
}
