#include<iostream>
#include<string>
using namespace std;
int main()
{
    string num;
    cin >> num;
    char temp = num[0];
    num[0] = num[num.length()-1];
    num[num.length()-1] = temp;
    cout <<num;
    
}
